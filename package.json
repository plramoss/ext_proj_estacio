{
  "scripts":{
    "start:frontend":"cd frontend && npm install && npx expo start" ,
    "generate_jwt_secret":"cd api && npm install && node generate-jwt-key.js" ,
    "start:api":"cd api && npm install && npm start"
  }
}
